// defaults

const defaultStyle = {
    fill: '#777777',
    stroke: '#ffffff',
    'stroke-width': 2
}

const defaultFont = {
    'family': 'Chalkduster',
    'size': '25px',
}

const defaultLayer = {
    'fill': 'white',
    'stroke': 'white',
    'stroke-width': '2',
    'stroke-opacity': '1.0',
    'style': 'visibility: visible;'
}

const viewBox = {
    width: 500,
    height: 600
}

export let defaults = {
    style: defaultStyle,
    font: defaultFont,
    layer: defaultLayer,
    viewBox: viewBox
}